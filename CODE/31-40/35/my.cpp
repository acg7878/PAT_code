#include <iostream>
#include <string>
#include <vector>
struct Account {
    std::string name;
    std::string password;
};

std::string check(std::string password){
    for(char ch : password){
    }
}

int main(){
    int n;
    std::cin >> n;
    std::vector<Account> accounts; 
    while (n--) {
        std::string name,password;
        std::cin >> name >> password;
        
    }
}